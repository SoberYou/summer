<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cq.summer.dao.SystemDao">

    <select id="test" parameterType="java.lang.String" resultType="java.lang.String">
        select name from test where id = 1
    </select>

    <select id="test1" parameterType="java.lang.String" resultType="java.lang.String">
        select name from test where id = 1
    </select>

    <select id="test2" parameterType="java.lang.String" resultType="java.lang.String">
        select name from test where id = 1
    </select>

</mapper>